# Example configuration without credentials
# Clients MUST provide credentials with each request

transport:
  type: stdio  # or "http"
  # http:
  #   host: localhost
  #   port: 3000

tools:
  # Jira configuration without credentials
  jira:
    base_url: https://your-domain.atlassian.net
    # No auth section - clients must provide credentials

  # Confluence configuration without credentials
  confluence:
    base_url: https://your-domain.atlassian.net
    # No auth section - clients must provide credentials

  # Bitbucket configuration without credentials (if using Bitbucket Server)
  # bitbucket:
  #   base_url: https://bitbucket.your-domain.com
  #   # No auth section - clients must provide credentials

  # Bamboo configuration without credentials (if using Bamboo)
  # bamboo:
  #   base_url: https://bamboo.your-domain.com
  #   # No auth section - clients must provide credentials
